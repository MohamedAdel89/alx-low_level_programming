/**
 *_abs - computes the absolute value
 *
 *@n: the integer number for the calculation
 *
 *Return: return the absolute value of the integer n
 */

int _abs(int n)
{
	if (n > 0)
		return (n);
	else
		return (-1 * n);
}
